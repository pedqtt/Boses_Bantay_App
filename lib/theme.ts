// Minimal theme tokens used across the app. Keep this file small and
// focused — screens import specific tokens rather than a huge theme.
export const colors = {
  primary: "#1D4ED8",
  onSurfaceFaint: "#4B5563",
};

export type ThemeColors = typeof colors;

export default { colors };
